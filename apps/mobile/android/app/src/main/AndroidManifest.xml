<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      No backup and no device-to-device transfer of app data: the WebView's cookie
      store holds the signed-in session, and restoring it onto another phone would
      carry the session with it. Cleartext is refused outright; the app only loads
      an https origin.
    -->
    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      What the implemented web features ask the WebView for. A page can only get
      what the app holds. Capacitor's BridgeWebChromeClient requests each one at
      runtime, at the moment the page asks, and hands a refusal back to the page:
      getUserMedia rejects, and geolocation fails with PERMISSION_DENIED. Each
      screen already explains a refusal. Nothing is requested at launch.

      CAMERA:   "Take photo" on lead documents, face enrolment, face check-in.
      LOCATION: attendance check-in/out and the site-visit GPS punch. Coarse is
                declared with fine so a user may grant only approximate location
                (Android 12+); the server decides whether that accuracy is enough.
      RECORD_AUDIO + MODIFY_AUDIO_SETTINGS: the live-call workspace's microphone
                mode and practice recordings. That is this phone's microphone only;
                it is not cellular call capture, and nothing here records a phone call.

      Not declared: background location (nothing tracks position after a punch) and
      notifications (push is not configured).
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!--
      Declaring a permission otherwise makes Play treat that hardware as required,
      and hides the app from devices without it. Office staff on a tablet with no
      camera still need the CRM.
    -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.location.network" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
</manifest>
