{
  "name": "master-app-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor shell for apps/web — development proof of concept (server.url is not production-supported).",
  "scripts": {
    "sync": "cap sync && node scripts/fix-ios-spm-paths.mjs",
    "open:android": "cap open android",
    "open:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
