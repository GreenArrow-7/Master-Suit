# Phone-first UI corrections — 20 Sep 2026

Owner's iPhone screenshots of YOUHAN ONE 1.0 (7) on `main` 3945893 were the defect evidence.
Branch `fix/mobile-shell-ux` off 3945893. Everything below is served by the web application:
the iOS and Android apps are WebViews on `https://one.youhan.in`, so a backend deploy delivers
every fix; no native rebuild is required for them (a rebuild only changes the embedded build
number).

| #   | Issue (screenshot)                                                 | Root cause                                                                                                                                                                                                | Fix                                                                                                                                                                                                                                                                                                | Screens                                                               | Verified                                                                                                         |
| --- | ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| 1   | Dark theme: header under the clock and Dynamic Island              | The phone rule `padding: 8px 12px 8px 54px` on `.lf-shell-topbar` reset the `padding-top: env(safe-area-inset-top)` declared earlier; only the light-surface stylesheet re-added the inset                | `styles/shell-mobile.css`: top bar `min-height`/`padding-top` carry the inset in every theme; landscape insets on the frame; applied once (native WebView adds none)                                                                                                                               | all                                                                   | Chromium (inset 0) light/dark/glass: padding-top ≥ 8px on 13 screens; **device check pending**                   |
| 2   | Grey "❮" tab at the right edge of every screen                     | Not an app element: hit-testing the right edge in Chromium at 390px finds only the page in every theme. It is the WebView/OS layer (iOS 26 back-swipe affordance in WKWebView with back-forward gestures) | None in the web app. Native option if unwanted: disable `allowsBackForwardNavigationGestures` on the Capacitor WebView                                                                                                                                                                             | all (device)                                                          | n/a                                                                                                              |
| 3   | Products' Columns panel off the left edge; Leads' below the screen | Popover was `position:absolute; right:0; width:320` inside a 46px anchor, and on Leads inside a `contain: paint` wrapper that became its containing block (measured x = −219 / −111, bottom 614+)         | `ColumnEditor.tsx` renders through a portal: bottom sheet on phones (heading, close, scrolling list, reachable actions, scrim, body scroll lock, Escape, focus return to the button or to the ••• summary that hid it), viewport-clamped popover on desktop; opening it closes the parent ••• menu | Leads, Products, every grid                                           | 320/390: sheet within the viewport both screens; 1280 popover under its button; `tests/e2e/mobile-shell.spec.ts` |
| 4   | Tab strip chevrons over labels ("‹ons")                            | Sticky 28px edge markers with the glyph and −30px margins sat on the first/last tab                                                                                                                       | Glyph removed on phones, fade kept at 18px, `scroll-padding-inline` keeps the active tab clear                                                                                                                                                                                                     | all tabbed areas                                                      | spec asserts no glyph in `::before`                                                                              |
| 5   | Two Create buttons on list screens                                 | Global "+ Create" and the page's own primary action competed for 390px                                                                                                                                    | `:has()` rule hides the global one on phones when the list header has a primary action; it stays where the page has none and always in the drawer                                                                                                                                                  | Leads, Projects, Products…                                            | spec: hidden on Leads, visible on Audit                                                                          |
| 6   | Long intro paragraphs                                              | Explanations written for desktop pushed the first action below the fold                                                                                                                                   | `PageIntro` component: title, one sentence, "How this works" disclosure (44px target, focus ring, themes); consent/approval warnings kept visible at the decision point                                                                                                                            | Audit, Roles, HR policy, Recruitment, Payroll, Integrations, Playbook | screenshots; spec asserts the disclosure is folded                                                               |
| 7   | Leads rows ~400px tall; audit rows lead with ids/paths             | Every column became a label/value pair on phones                                                                                                                                                          | `columnPriority()` (explicit `priority` or heuristic): primary + secondary shown, the rest behind a native "Details" disclosure per row; audit columns re-prioritised (readable summary first, raw event key in details); desktop unchanged                                                        | every `ConfigurableGrid`/`WorkspaceTable` list                        | Leads 162→118px collapsed, Audit 175→115px; no horizontal overflow                                               |
| 8   | Payroll date inputs overflow the card                              | `input[type=date]` intrinsic width on iOS; `.lf-input` never set `min-width:0`/`appearance:none`                                                                                                          | `styles/forms-mobile.css`: block-level 100% controls, `appearance:none`, single-column form grid on phones, actions block at the end of the form (no fixed footer), `scroll-margin` for focused fields                                                                                             | every `WorkspaceRecordForm`                                           | 320/360/390 inside the card; **iOS date-picker width: device check**                                             |
| 9   | Projects: filters swallow the screen, uneven widths                | Seven controls in a wrapping flex row, hard-coded 140px                                                                                                                                                   | Search stays; facets in `FilterSheet` with an active count and Clear; desktop inline                                                                                                                                                                                                               | Projects                                                              | 360/390 screenshots                                                                                              |
| 10  | Summary/Recruitment tiles one per row                              | `auto-fill minmax(180px)` grid                                                                                                                                                                            | 2-column metric grid ≥360px, 1 column at 320, no fixed heights                                                                                                                                                                                                                                     | Summary, Recruitment                                                  | screenshots                                                                                                      |
| 11  | Reports metrics split from their labels                            | Hairline flex "sentence" strip wrapped                                                                                                                                                                    | 2-column tiles keeping label+value+% together; funnel stays a real table in its scroll wrap                                                                                                                                                                                                        | Reports                                                               | screenshots                                                                                                      |
| 12  | Playbook button squeezes the text                                  | Side-by-side header at phone width                                                                                                                                                                        | Action stacks below the description ≤760px                                                                                                                                                                                                                                                         | Playbook                                                              | screenshot                                                                                                       |
| 13  | Empty states advertise missing actions                             | Copy assumed buttons that some roles/screens don't show                                                                                                                                                   | Copy depends on filters and `CREATE` permission; Products no longer says "add a product"                                                                                                                                                                                                           | Projects, Products                                                    | screenshots                                                                                                      |
| 14  | (found) `/sales/projects?status=ACTIVE` returned 500               | Unknown enum value passed straight to Prisma                                                                                                                                                              | Values outside `PROJECT_STATUSES`/`POSSESSION_STATUSES` are ignored                                                                                                                                                                                                                                | Projects                                                              | `tests/unit/catalogue-filters.spec.ts`                                                                           |

Verification widths: 320, 360, 390, 430 (phone, light/dark/glass), 768, 1024, 1280 (desktop
popover unchanged). Captures in `C:\ymw\ui-before` and `C:\ymw\ui-after-*` on the rig.
Not verifiable in Chromium and marked for the device pass: the real status-bar inset, the
iOS date picker's intrinsic width, and the on-screen keyboard's effect on sheets and forms.

Regression specs: `tests/e2e/mobile-shell.spec.ts` (new), plus lead-import, lead-close-out,
business-journey, assisted-call and workspace-summary-sample (unchanged expectations).
