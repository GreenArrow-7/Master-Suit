# YOUHAN ONE — default roles and what each can do

_Generated by `scripts/roles-doc.ts` from `prisma/seed/roles.ts` and the permission catalogue. Do not edit by hand; run the script._

Rank: lower is more senior; a person can only create, edit or assign roles below their own rank.
Scope: how far the role sees — own records, team, branch, region or the whole workspace. Scope is per permission, not per role.
A workspace administrator can change any of this in **Admin → Roles & permissions**; this document describes the defaults a new workspace starts with.

## Super Administrator (`super_admin`, rank 0, default scope whole workspace)

Modules: `accounts`, `activities`, `agencyfee`, `allocation`, `apikeys`, `attendance`, `auditlogs`, `automation`, `bookings`, `calls`, `campaigns`, `clientprofiles`, `collections`, `commissions`, `commissionslabs`, `communications`, `contacts`, `contests`, `dashboards`, `departments`, `dialer`, `distribution`, `documents`, `employee`, `events`, `exports`, `fieldsales`, `forms`, `holidays`, `hr_documents`, `hr_reports`, `hrms`, `imports`, `integrations`, `landingpages`, `leads`, `leave`, `listings`, `lists`, `opportunities`, `overtime`, `payouts`, `payroll`, `performance`, `posts`, `products`, `projects`, `recruitment`, `referrals`, `reports`, `requirements`, `roles`, `settings`, `shifts`, `sla`, `smartviews`, `tasks`, `testimonials`, `tickets`, `users`, `visits`, `work_locations`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `accounts` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | manage configuration (whole workspace), manage users (whole workspace) |
| `activities` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | manage configuration (whole workspace), manage users (whole workspace) |
| `agencyfee` | view (whole workspace) | create (whole workspace) | — | approve (whole workspace) | — | — |
| `allocation` | view (whole workspace) | create (whole workspace) | — | approve (whole workspace) | — | — |
| `apikeys` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `attendance` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | approve (whole workspace) | — | manage configuration (whole workspace), manage users (whole workspace) |
| `auditlogs` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `automation` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `bookings` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | — |
| `calls` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | manage configuration (whole workspace), manage users (whole workspace) |
| `campaigns` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | manage configuration (whole workspace), manage users (whole workspace) |
| `clientprofiles` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | — |
| `collections` | view (whole workspace) | create (whole workspace) | — | approve (whole workspace) | — | — |
| `commissions` | view (whole workspace) | edit (whole workspace) | — | approve (whole workspace) | export (whole workspace) | — |
| `commissionslabs` | view (whole workspace) | edit (whole workspace) | — | approve (whole workspace) | — | — |
| `communications` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `contacts` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | manage configuration (whole workspace), manage users (whole workspace) |
| `contests` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | — |
| `dashboards` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `departments` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | manage configuration (whole workspace), manage users (whole workspace) |
| `dialer` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | manage users (whole workspace) |
| `distribution` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `documents` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `employee` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | manage configuration (whole workspace), manage users (whole workspace) |
| `events` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `exports` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `fieldsales` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `forms` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `holidays` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | manage configuration (whole workspace), manage users (whole workspace) |
| `hr_documents` | view (whole workspace), view sensitive fields (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | manage configuration (whole workspace), manage users (whole workspace) |
| `hr_reports` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | manage configuration (whole workspace), manage users (whole workspace) |
| `hrms` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | manage configuration (whole workspace), manage users (whole workspace) |
| `imports` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `integrations` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `landingpages` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `leads` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | manage configuration (whole workspace), manage users (whole workspace) |
| `leave` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | approve (whole workspace) | — | manage configuration (whole workspace), manage users (whole workspace) |
| `listings` | view (whole workspace), view sensitive fields (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `lists` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `opportunities` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | manage configuration (whole workspace), manage users (whole workspace) |
| `overtime` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | approve (whole workspace) | — | manage configuration (whole workspace), manage users (whole workspace) |
| `payouts` | view (whole workspace) | create (whole workspace) | — | approve (whole workspace) | — | — |
| `payroll` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | approve (whole workspace) | export (whole workspace) | — |
| `performance` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | approve (whole workspace) | — | — |
| `posts` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | — |
| `products` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `projects` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `recruitment` | view (whole workspace), view sensitive fields (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | approve (whole workspace) | — | — |
| `referrals` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | — |
| `reports` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `requirements` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | — |
| `roles` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `settings` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `shifts` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | approve (whole workspace) | — | manage configuration (whole workspace), manage users (whole workspace) |
| `sla` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `smartviews` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `tasks` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | manage configuration (whole workspace), manage users (whole workspace) |
| `testimonials` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | approve (whole workspace) | — | — |
| `tickets` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `users` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `visits` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | approve (whole workspace) | — | manage users (whole workspace) |
| `work_locations` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | manage configuration (whole workspace), manage users (whole workspace) |

## Organization Administrator (`org_admin`, rank 10, default scope whole workspace)

Modules: `accounts`, `activities`, `agencyfee`, `allocation`, `apikeys`, `attendance`, `auditlogs`, `automation`, `bookings`, `calls`, `campaigns`, `clientprofiles`, `collections`, `commissions`, `commissionslabs`, `communications`, `contacts`, `contests`, `dashboards`, `departments`, `dialer`, `distribution`, `documents`, `employee`, `events`, `exports`, `fieldsales`, `forms`, `holidays`, `hr_documents`, `hr_reports`, `hrms`, `imports`, `integrations`, `landingpages`, `leads`, `leave`, `listings`, `lists`, `opportunities`, `overtime`, `payouts`, `payroll`, `performance`, `posts`, `products`, `projects`, `recruitment`, `referrals`, `reports`, `requirements`, `roles`, `settings`, `shifts`, `sla`, `smartviews`, `tasks`, `testimonials`, `tickets`, `users`, `visits`, `work_locations`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `accounts` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | manage configuration (whole workspace), manage users (whole workspace) |
| `activities` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | manage configuration (whole workspace), manage users (whole workspace) |
| `agencyfee` | view (whole workspace) | create (whole workspace) | — | approve (whole workspace) | — | — |
| `allocation` | view (whole workspace) | create (whole workspace) | — | approve (whole workspace) | — | — |
| `apikeys` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `attendance` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | approve (whole workspace) | — | manage configuration (whole workspace), manage users (whole workspace) |
| `auditlogs` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `automation` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `bookings` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | — |
| `calls` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | manage configuration (whole workspace), manage users (whole workspace) |
| `campaigns` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | manage configuration (whole workspace), manage users (whole workspace) |
| `clientprofiles` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | — |
| `collections` | view (whole workspace) | create (whole workspace) | — | approve (whole workspace) | — | — |
| `commissions` | view (whole workspace) | edit (whole workspace) | — | approve (whole workspace) | export (whole workspace) | — |
| `commissionslabs` | view (whole workspace) | edit (whole workspace) | — | approve (whole workspace) | — | — |
| `communications` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `contacts` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | manage configuration (whole workspace), manage users (whole workspace) |
| `contests` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | — |
| `dashboards` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `departments` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | manage configuration (whole workspace), manage users (whole workspace) |
| `dialer` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | manage users (whole workspace) |
| `distribution` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `documents` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `employee` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | manage configuration (whole workspace), manage users (whole workspace) |
| `events` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `exports` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `fieldsales` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `forms` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `holidays` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | manage configuration (whole workspace), manage users (whole workspace) |
| `hr_documents` | view (whole workspace), view sensitive fields (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | manage configuration (whole workspace), manage users (whole workspace) |
| `hr_reports` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | manage configuration (whole workspace), manage users (whole workspace) |
| `hrms` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | manage configuration (whole workspace), manage users (whole workspace) |
| `imports` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `integrations` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `landingpages` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `leads` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | manage configuration (whole workspace), manage users (whole workspace) |
| `leave` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | approve (whole workspace) | — | manage configuration (whole workspace), manage users (whole workspace) |
| `listings` | view (whole workspace), view sensitive fields (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `lists` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `opportunities` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | manage configuration (whole workspace), manage users (whole workspace) |
| `overtime` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | approve (whole workspace) | — | manage configuration (whole workspace), manage users (whole workspace) |
| `payouts` | view (whole workspace) | create (whole workspace) | — | approve (whole workspace) | — | — |
| `payroll` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | approve (whole workspace) | export (whole workspace) | — |
| `performance` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | approve (whole workspace) | — | — |
| `posts` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | — |
| `products` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `projects` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `recruitment` | view (whole workspace), view sensitive fields (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | approve (whole workspace) | — | — |
| `referrals` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | — |
| `reports` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `requirements` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | — |
| `roles` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `settings` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `shifts` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | approve (whole workspace) | — | manage configuration (whole workspace), manage users (whole workspace) |
| `sla` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `smartviews` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `tasks` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | manage configuration (whole workspace), manage users (whole workspace) |
| `testimonials` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | approve (whole workspace) | — | — |
| `tickets` | view (whole workspace), view sensitive fields (whole workspace) | assign (whole workspace), bulk update (whole workspace), create (whole workspace), edit (whole workspace), import (whole workspace), reassign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `users` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace), view reports (whole workspace) | access api (whole workspace), manage automation (whole workspace), manage configuration (whole workspace), manage users (whole workspace) |
| `visits` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | approve (whole workspace) | — | manage users (whole workspace) |
| `work_locations` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | manage configuration (whole workspace), manage users (whole workspace) |

## Executive (Read-Only) (`executive_read_only`, rank 15, default scope whole workspace)

Modules: `accounts`, `activities`, `calls`, `campaigns`, `contacts`, `dashboards`, `documents`, `leads`, `opportunities`, `products`, `reports`, `smartviews`, `tasks`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `accounts` | view (whole workspace) | — | — | — | — | — |
| `activities` | view (whole workspace) | — | — | — | — | — |
| `calls` | view (whole workspace) | — | — | — | — | — |
| `campaigns` | view (whole workspace) | — | — | — | — | — |
| `contacts` | view (whole workspace) | — | — | — | — | — |
| `dashboards` | view (whole workspace) | — | — | — | — | — |
| `documents` | view (whole workspace) | — | — | — | — | — |
| `leads` | view (whole workspace) | — | — | — | — | — |
| `opportunities` | view (whole workspace) | — | — | — | — | — |
| `products` | view (whole workspace) | — | — | — | — | — |
| `reports` | view (whole workspace) | — | — | — | view reports (whole workspace) | — |
| `smartviews` | view (whole workspace) | — | — | — | — | — |
| `tasks` | view (whole workspace) | — | — | — | — | — |

## Sales Director (`sales_director`, rank 20, default scope whole workspace)

Modules: `accounts`, `activities`, `agencyfee`, `campaigns`, `contacts`, `dashboards`, `distribution`, `documents`, `fieldsales`, `leads`, `opportunities`, `products`, `reports`, `smartviews`, `tasks`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `accounts` | view (whole workspace) | edit (whole workspace) | — | — | — | — |
| `activities` | view (whole workspace) | create (whole workspace) | — | — | — | — |
| `agencyfee` | view (whole workspace) | create (whole workspace) | — | — | — | — |
| `campaigns` | view (whole workspace) | — | — | — | — | — |
| `contacts` | view (whole workspace) | — | — | — | — | — |
| `dashboards` | view (whole workspace) | create (whole workspace) | — | — | — | — |
| `distribution` | view (whole workspace) | — | — | — | — | manage configuration (whole workspace) |
| `documents` | view (whole workspace) | — | — | — | — | — |
| `fieldsales` | view (whole workspace) | — | — | — | — | — |
| `leads` | view (whole workspace), view sensitive fields (whole workspace) | create (whole workspace), edit (whole workspace), assign (whole workspace), reassign (whole workspace), bulk update (whole workspace), import (whole workspace) | — | — | export (whole workspace) | — |
| `opportunities` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `products` | view (whole workspace) | edit (whole workspace) | — | — | — | — |
| `reports` | view (whole workspace) | create (whole workspace) | — | — | view reports (whole workspace) | — |
| `smartviews` | view (whole workspace) | create (whole workspace) | — | — | — | — |
| `tasks` | view (whole workspace) | assign (whole workspace) | — | — | — | — |

## HR Administrator (`hr_admin`, rank 20, default scope whole workspace)

Modules: `attendance`, `auditlogs`, `dashboards`, `departments`, `documents`, `employee`, `employees`, `holidays`, `hr_documents`, `hr_reports`, `hrms`, `leave`, `overtime`, `payroll`, `performance`, `recruitment`, `reports`, `roles`, `shifts`, `users`, `work_locations`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `attendance` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | approve (whole workspace) | export (whole workspace), view reports (whole workspace) | — |
| `auditlogs` | view (whole workspace) | — | — | — | — | — |
| `dashboards` | view (whole workspace) | — | — | — | — | — |
| `departments` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | — |
| `documents` | view (whole workspace) | create (whole workspace) | — | — | — | — |
| `employee` | view (whole workspace), view sensitive fields (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `employees` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | export (whole workspace) | — |
| `holidays` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | — |
| `hr_documents` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |
| `hr_reports` | view (whole workspace) | — | — | — | view reports (whole workspace), export (whole workspace) | — |
| `hrms` | view (whole workspace) | edit (whole workspace) | — | — | — | manage configuration (whole workspace) |
| `leave` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | approve (whole workspace) | export (whole workspace) | — |
| `overtime` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | approve (whole workspace) | — | — |
| `payroll` | view (whole workspace) | — | — | — | — | — |
| `performance` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | approve (whole workspace) | — | — |
| `recruitment` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | approve (whole workspace) | — | — |
| `reports` | view (whole workspace) | — | — | — | view reports (whole workspace) | — |
| `roles` | view (whole workspace) | assign (whole workspace) | — | — | — | — |
| `shifts` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | approve (whole workspace) | — | — |
| `users` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | manage users (whole workspace) |
| `work_locations` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | manage configuration (whole workspace) |

## Regional Manager (`regional_manager`, rank 30, default scope region)

Modules: `accounts`, `activities`, `contacts`, `dashboards`, `documents`, `fieldsales`, `leads`, `opportunities`, `products`, `reports`, `smartviews`, `tasks`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `accounts` | view (region) | edit (region) | — | — | — | — |
| `activities` | view (region) | create (region) | — | — | — | — |
| `contacts` | view (region) | — | — | — | — | — |
| `dashboards` | view (region) | create (region) | — | — | — | — |
| `documents` | view (region) | — | — | — | — | — |
| `fieldsales` | view (region) | — | — | — | — | — |
| `leads` | view (region), view sensitive fields (region) | create (region), edit (region), assign (region), reassign (region), bulk update (region), import (region) | — | — | export (region) | — |
| `opportunities` | view (region) | create (region), edit (region) | — | — | — | — |
| `products` | view (whole workspace) | — | — | — | — | — |
| `reports` | view (region) | create (region) | — | — | view reports (region) | — |
| `smartviews` | view (region) | create (region) | — | — | — | — |
| `tasks` | view (region) | assign (region) | — | — | — | — |

## IT Administrator (`it_admin`, rank 30, default scope whole workspace)

Modules: `apikeys`, `auditlogs`, `automation`, `dashboards`, `exports`, `imports`, `integrations`, `roles`, `settings`, `users`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `apikeys` | view (whole workspace) | create (whole workspace) | delete (whole workspace) | — | — | access api (whole workspace) |
| `auditlogs` | view (whole workspace) | — | — | — | export (whole workspace) | — |
| `automation` | view (whole workspace) | — | — | — | — | manage automation (whole workspace) |
| `dashboards` | view (whole workspace) | — | — | — | — | — |
| `exports` | view (whole workspace) | — | — | — | export (whole workspace) | — |
| `imports` | view (whole workspace) | import (whole workspace) | — | — | — | — |
| `integrations` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | manage configuration (whole workspace) |
| `roles` | view (whole workspace) | assign (whole workspace) | — | — | — | — |
| `settings` | view (whole workspace) | edit (whole workspace) | — | — | — | manage configuration (whole workspace) |
| `users` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | manage users (whole workspace) |

## Finance Administrator (`finance_admin`, rank 35, default scope whole workspace)

Modules: `agencyfee`, `auditlogs`, `collections`, `commissions`, `commissionslabs`, `dashboards`, `exports`, `hr_reports`, `overtime`, `payouts`, `payroll`, `reports`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `agencyfee` | view (whole workspace) | — | — | approve (whole workspace) | — | — |
| `auditlogs` | view (whole workspace) | — | — | — | — | — |
| `collections` | view (whole workspace) | create (whole workspace) | — | approve (whole workspace) | — | — |
| `commissions` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | approve (whole workspace) | export (whole workspace) | — |
| `commissionslabs` | view (whole workspace) | edit (whole workspace) | — | — | — | — |
| `dashboards` | view (whole workspace) | — | — | — | — | — |
| `exports` | view (whole workspace) | — | — | — | export (whole workspace) | — |
| `hr_reports` | view (whole workspace) | — | — | — | view reports (whole workspace), export (whole workspace) | — |
| `overtime` | view (whole workspace) | — | — | approve (whole workspace) | — | — |
| `payouts` | view (whole workspace) | — | — | approve (whole workspace) | export (whole workspace) | — |
| `payroll` | view (whole workspace), view sensitive fields (whole workspace) | — | — | approve (whole workspace) | export (whole workspace) | — |
| `reports` | view (whole workspace) | — | — | — | view reports (whole workspace), export (whole workspace) | — |

## Branch Manager (`branch_manager`, rank 40, default scope branch)

Modules: `accounts`, `activities`, `contacts`, `dashboards`, `documents`, `fieldsales`, `leads`, `opportunities`, `products`, `reports`, `smartviews`, `tasks`, `tickets`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `accounts` | view (branch) | edit (branch) | — | — | — | — |
| `activities` | view (branch) | create (branch) | — | — | — | — |
| `contacts` | view (branch) | — | — | — | — | — |
| `dashboards` | view (branch) | create (branch) | — | — | — | — |
| `documents` | view (branch) | — | — | — | — | — |
| `fieldsales` | view (branch) | — | — | — | — | — |
| `leads` | view (branch), view sensitive fields (branch) | create (branch), edit (branch), assign (branch), reassign (branch), bulk update (branch), import (branch) | — | — | export (branch) | — |
| `opportunities` | view (branch) | create (branch), edit (branch) | — | — | — | — |
| `products` | view (whole workspace) | — | — | — | — | — |
| `reports` | view (branch) | create (branch) | — | — | view reports (branch) | — |
| `smartviews` | view (branch) | create (branch) | — | — | — | — |
| `tasks` | view (branch) | assign (branch) | — | — | — | — |
| `tickets` | view (branch) | — | — | — | — | — |

## Marketing Manager (`marketing_manager`, rank 40, default scope whole workspace)

Modules: `accounts`, `activities`, `automation`, `campaigns`, `communications`, `contacts`, `dashboards`, `forms`, `landingpages`, `leads`, `lists`, `opportunities`, `products`, `reports`, `smartviews`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `accounts` | view (whole workspace) | — | — | — | — | — |
| `activities` | view (whole workspace) | create (whole workspace) | — | — | — | — |
| `automation` | view (whole workspace) | — | — | — | — | manage automation (whole workspace) |
| `campaigns` | view (whole workspace) | create (whole workspace), edit (whole workspace) | delete (whole workspace) | — | — | — |
| `communications` | view (whole workspace) | create (whole workspace) | — | — | — | — |
| `contacts` | view (whole workspace) | — | — | — | — | — |
| `dashboards` | view (whole workspace) | create (whole workspace) | — | — | — | — |
| `forms` | view (whole workspace) | create (whole workspace) | — | — | — | manage configuration (whole workspace) |
| `landingpages` | view (whole workspace) | create (whole workspace) | — | — | — | manage configuration (whole workspace) |
| `leads` | view (whole workspace) | create (whole workspace), edit (team), bulk update (team), import (whole workspace) | — | — | export (whole workspace) | — |
| `lists` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | — |
| `opportunities` | view (whole workspace) | — | — | — | — | — |
| `products` | view (whole workspace) | — | — | — | — | — |
| `reports` | view (whole workspace) | create (whole workspace) | — | — | view reports (whole workspace) | — |
| `smartviews` | view (whole workspace) | create (whole workspace) | — | — | — | — |

## Customer Service Manager (`service_manager`, rank 40, default scope whole workspace)

Modules: `accounts`, `activities`, `automation`, `communications`, `contacts`, `dashboards`, `documents`, `forms`, `leads`, `products`, `reports`, `sla`, `smartviews`, `tasks`, `tickets`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `accounts` | view (whole workspace) | — | — | — | — | — |
| `activities` | view (branch) | create (branch) | — | — | — | — |
| `automation` | view (whole workspace) | — | — | — | — | manage automation (whole workspace) |
| `communications` | view (whole workspace) | create (whole workspace) | — | — | — | — |
| `contacts` | view (whole workspace) | — | — | — | — | — |
| `dashboards` | view (whole workspace) | create (whole workspace) | — | — | — | — |
| `documents` | view (branch) | — | — | — | — | — |
| `forms` | view (whole workspace) | — | — | — | — | manage configuration (whole workspace) |
| `leads` | view (branch) | — | — | — | — | — |
| `products` | view (whole workspace) | — | — | — | — | — |
| `reports` | view (whole workspace) | create (whole workspace) | — | — | view reports (whole workspace) | — |
| `sla` | view (whole workspace) | — | — | — | — | manage configuration (whole workspace) |
| `smartviews` | view (whole workspace) | — | — | — | — | — |
| `tasks` | view (branch) | assign (branch) | — | — | — | — |
| `tickets` | view (whole workspace) | create (whole workspace), edit (whole workspace), assign (whole workspace) | delete (whole workspace) | — | export (whole workspace) | — |

## Call Quality Manager (`call_qa`, rank 40, default scope whole workspace)

Modules: `calls`, `dashboards`, `documents`, `leads`, `reports`, `tasks`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `calls` | view (whole workspace) | edit (whole workspace) | — | — | export (whole workspace) | — |
| `dashboards` | view (whole workspace) | — | — | — | — | — |
| `documents` | view (whole workspace) | — | — | — | — | — |
| `leads` | view (whole workspace) | — | — | — | — | — |
| `reports` | view (whole workspace) | — | — | — | view reports (whole workspace) | — |
| `tasks` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | — |

## Reporting Analyst (`analyst`, rank 45, default scope whole workspace)

Modules: `accounts`, `activities`, `auditlogs`, `calls`, `campaigns`, `contacts`, `dashboards`, `documents`, `events`, `fieldsales`, `leads`, `opportunities`, `products`, `reports`, `smartviews`, `tasks`, `tickets`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `accounts` | view (whole workspace) | — | — | — | — | — |
| `activities` | view (whole workspace) | — | — | — | — | — |
| `auditlogs` | view (whole workspace) | — | — | — | — | — |
| `calls` | view (whole workspace) | — | — | — | — | — |
| `campaigns` | view (whole workspace) | — | — | — | — | — |
| `contacts` | view (whole workspace) | — | — | — | — | — |
| `dashboards` | view (whole workspace) | create (whole workspace) | — | — | — | — |
| `documents` | view (whole workspace) | — | — | — | — | — |
| `events` | view (whole workspace) | — | — | — | — | — |
| `fieldsales` | view (whole workspace) | — | — | — | — | — |
| `leads` | view (whole workspace) | — | — | — | export (whole workspace) | — |
| `opportunities` | view (whole workspace) | — | — | — | export (whole workspace) | — |
| `products` | view (whole workspace) | — | — | — | — | — |
| `reports` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | export (whole workspace), view reports (whole workspace) | — |
| `smartviews` | view (whole workspace) | create (whole workspace) | — | — | — | — |
| `tasks` | view (whole workspace) | — | — | — | — | — |
| `tickets` | view (whole workspace) | — | — | — | — | — |

## Department Manager (`dept_manager`, rank 45, default scope team)

Modules: `attendance`, `dashboards`, `employee`, `employees`, `hr_reports`, `hrms`, `leave`, `overtime`, `performance`, `shifts`, `tasks`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `attendance` | view (team) | — | — | approve (team) | view reports (team) | — |
| `dashboards` | view (team) | — | — | — | — | — |
| `employee` | view (team) | — | — | — | — | — |
| `employees` | view (team) | — | — | — | — | — |
| `hr_reports` | view (team) | — | — | — | — | — |
| `hrms` | view (team) | — | — | — | — | — |
| `leave` | view (team) | — | — | approve (team) | — | — |
| `overtime` | view (team) | — | — | approve (team) | — | — |
| `performance` | view (team) | create (team), edit (team) | — | — | — | — |
| `shifts` | view (team) | edit (team) | — | — | — | — |
| `tasks` | view (team) | assign (team) | — | — | — | — |

## Site Manager (`site_manager`, rank 48, default scope branch)

Modules: `attendance`, `dashboards`, `employee`, `employees`, `hr_reports`, `hrms`, `leave`, `overtime`, `shifts`, `work_locations`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `attendance` | view (branch) | — | — | approve (branch) | view reports (branch) | — |
| `dashboards` | view (branch) | — | — | — | — | — |
| `employee` | view (branch) | — | — | — | — | — |
| `employees` | view (branch) | — | — | — | — | — |
| `hr_reports` | view (branch) | — | — | — | — | — |
| `hrms` | view (branch) | — | — | — | — | — |
| `leave` | view (branch) | — | — | approve (branch) | — | — |
| `overtime` | view (branch) | — | — | approve (branch) | — | — |
| `shifts` | view (branch) | create (branch), edit (branch) | — | — | — | — |
| `work_locations` | view (branch) | — | — | — | — | — |

## Team Manager (`team_manager`, rank 50, default scope team)

Modules: `accounts`, `activities`, `contacts`, `dashboards`, `documents`, `fieldsales`, `leads`, `opportunities`, `products`, `reports`, `smartviews`, `tasks`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `accounts` | view (team) | edit (team) | — | — | — | — |
| `activities` | view (team) | create (team) | — | — | — | — |
| `contacts` | view (team) | — | — | — | — | — |
| `dashboards` | view (team) | — | — | — | — | — |
| `documents` | view (team) | — | — | — | — | — |
| `fieldsales` | view (team) | — | — | — | — | — |
| `leads` | view (team) | create (team), edit (team), assign (team), reassign (team), bulk update (team) | — | — | — | — |
| `opportunities` | view (team) | create (team), edit (team) | — | — | — | — |
| `products` | view (whole workspace) | — | — | — | — | — |
| `reports` | view (team) | — | — | — | view reports (team) | — |
| `smartviews` | view (team) | create (team) | — | — | — | — |
| `tasks` | view (team) | assign (team) | — | — | — | — |

## HR Executive (`hr_executive`, rank 50, default scope whole workspace)

Modules: `attendance`, `dashboards`, `departments`, `employee`, `employees`, `holidays`, `hr_documents`, `hr_reports`, `hrms`, `leave`, `overtime`, `performance`, `recruitment`, `shifts`, `users`, `work_locations`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `attendance` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | — |
| `dashboards` | view (whole workspace) | — | — | — | — | — |
| `departments` | view (whole workspace) | — | — | — | — | — |
| `employee` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | — |
| `employees` | view (whole workspace) | edit (whole workspace) | — | — | — | — |
| `holidays` | view (whole workspace) | — | — | — | — | — |
| `hr_documents` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | — |
| `hr_reports` | view (whole workspace) | — | — | — | — | — |
| `hrms` | view (whole workspace) | — | — | — | — | — |
| `leave` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | — |
| `overtime` | view (whole workspace) | create (whole workspace) | — | — | — | — |
| `performance` | view (whole workspace) | create (whole workspace) | — | — | — | — |
| `recruitment` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | — |
| `shifts` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | — | — |
| `users` | view (whole workspace) | — | — | — | — | — |
| `work_locations` | view (whole workspace) | — | — | — | — | — |

## Payroll Officer (`payroll_officer`, rank 55, default scope whole workspace)

Modules: `attendance`, `dashboards`, `employee`, `employees`, `hr_reports`, `hrms`, `leave`, `overtime`, `payroll`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `attendance` | view (whole workspace) | — | — | — | view reports (whole workspace), export (whole workspace) | — |
| `dashboards` | view (whole workspace) | — | — | — | — | — |
| `employee` | view (whole workspace), view sensitive fields (whole workspace) | — | — | — | — | — |
| `employees` | view (whole workspace) | — | — | — | — | — |
| `hr_reports` | view (whole workspace) | — | — | — | view reports (whole workspace), export (whole workspace) | — |
| `hrms` | view (whole workspace) | — | — | — | — | — |
| `leave` | view (whole workspace) | — | — | — | — | — |
| `overtime` | view (whole workspace) | — | — | — | — | — |
| `payroll` | view (whole workspace), view sensitive fields (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | export (whole workspace) | — |

## Sales Representative (`sales_rep`, rank 60, default scope own records)

Modules: `accounts`, `activities`, `contacts`, `dashboards`, `documents`, `leads`, `opportunities`, `products`, `reports`, `smartviews`, `tasks`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `accounts` | view (team) | — | — | — | — | — |
| `activities` | view (own records) | create (own records) | — | — | — | — |
| `contacts` | view (team) | — | — | — | — | — |
| `dashboards` | view (own records) | — | — | — | — | — |
| `documents` | view (own records) | — | — | — | — | — |
| `leads` | view (own records) | create (own records), edit (own records) | — | — | — | — |
| `opportunities` | view (own records) | create (own records), edit (own records) | — | — | — | — |
| `products` | view (whole workspace) | — | — | — | — | — |
| `reports` | view (own records) | — | — | — | view reports (own records) | — |
| `smartviews` | view (own records) | create (own records) | — | — | — | — |
| `tasks` | view (own records) | create (own records), edit (own records) | — | — | — | — |

## Field Sales Representative (`field_rep`, rank 60, default scope own records)

Modules: `accounts`, `activities`, `contacts`, `documents`, `fieldsales`, `leads`, `opportunities`, `products`, `smartviews`, `tasks`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `accounts` | view (team) | — | — | — | — | — |
| `activities` | view (own records) | create (own records) | — | — | — | — |
| `contacts` | view (team) | — | — | — | — | — |
| `documents` | view (own records) | — | — | — | — | — |
| `fieldsales` | view (own records) | create (own records), edit (own records) | — | — | — | — |
| `leads` | view (own records) | create (own records), edit (own records) | — | — | — | — |
| `opportunities` | view (own records) | edit (own records) | — | — | — | — |
| `products` | view (whole workspace) | — | — | — | — | — |
| `smartviews` | view (own records) | — | — | — | — | — |
| `tasks` | view (own records) | create (own records), edit (own records) | — | — | — | — |

## Marketing Executive (`marketing_exec`, rank 60, default scope own records)

Modules: `activities`, `campaigns`, `communications`, `contacts`, `leads`, `lists`, `products`, `reports`, `smartviews`, `tasks`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `activities` | view (own records) | create (own records) | — | — | — | — |
| `campaigns` | view (own records) | create (own records), edit (own records) | — | — | — | — |
| `communications` | view (own records) | create (own records) | — | — | — | — |
| `contacts` | view (team) | — | — | — | — | — |
| `leads` | view (team) | create (team), edit (own records) | — | — | — | — |
| `lists` | view (own records) | create (own records) | — | — | — | — |
| `products` | view (whole workspace) | — | — | — | — | — |
| `reports` | view (own records) | — | — | — | view reports (own records) | — |
| `smartviews` | view (own records) | — | — | — | — | — |
| `tasks` | view (own records) | — | — | — | — | — |

## Customer Service Agent (`service_agent`, rank 60, default scope team)

Modules: `accounts`, `activities`, `communications`, `contacts`, `documents`, `leads`, `products`, `reports`, `smartviews`, `tasks`, `tickets`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `accounts` | view (team) | — | — | — | — | — |
| `activities` | view (team) | create (team) | — | — | — | — |
| `communications` | view (team) | create (team) | — | — | — | — |
| `contacts` | view (team) | — | — | — | — | — |
| `documents` | view (team) | — | — | — | — | — |
| `leads` | view (team) | — | — | — | — | — |
| `products` | view (whole workspace) | — | — | — | — | — |
| `reports` | view (team) | — | — | — | view reports (team) | — |
| `smartviews` | view (team) | — | — | — | — | — |
| `tasks` | view (team) | — | — | — | — | — |
| `tickets` | view (team) | create (team), edit (team) | — | — | — | — |

## Recruiter (`recruiter`, rank 60, default scope whole workspace)

Modules: `dashboards`, `departments`, `employee`, `employees`, `hr_documents`, `hrms`, `recruitment`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `dashboards` | view (whole workspace) | — | — | — | — | — |
| `departments` | view (whole workspace) | — | — | — | — | — |
| `employee` | view (whole workspace) | — | — | — | — | — |
| `employees` | view (whole workspace) | — | — | — | — | — |
| `hr_documents` | view (whole workspace) | create (whole workspace) | — | — | — | — |
| `hrms` | view (whole workspace) | — | — | — | — | — |
| `recruitment` | view (whole workspace) | create (whole workspace), edit (whole workspace) | — | — | export (whole workspace) | — |

## Auditor (`auditor`, rank 70, default scope whole workspace)

Modules: `attendance`, `auditlogs`, `dashboards`, `employee`, `employees`, `hr_documents`, `hr_reports`, `hrms`, `leave`, `overtime`, `payroll`, `reports`, `roles`, `settings`, `users`, `work_locations`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `attendance` | view (whole workspace) | — | — | — | view reports (whole workspace), export (whole workspace) | — |
| `auditlogs` | view (whole workspace) | — | — | — | export (whole workspace) | — |
| `dashboards` | view (whole workspace) | — | — | — | — | — |
| `employee` | view (whole workspace) | — | — | — | — | — |
| `employees` | view (whole workspace) | — | — | — | — | — |
| `hr_documents` | view (whole workspace) | — | — | — | — | — |
| `hr_reports` | view (whole workspace) | — | — | — | view reports (whole workspace), export (whole workspace) | — |
| `hrms` | view (whole workspace) | — | — | — | — | — |
| `leave` | view (whole workspace) | — | — | — | — | — |
| `overtime` | view (whole workspace) | — | — | — | — | — |
| `payroll` | view (whole workspace) | — | — | — | — | — |
| `reports` | view (whole workspace) | — | — | — | view reports (whole workspace) | — |
| `roles` | view (whole workspace) | — | — | — | — | — |
| `settings` | view (whole workspace) | — | — | — | — | — |
| `users` | view (whole workspace) | — | — | — | — | — |
| `work_locations` | view (whole workspace) | — | — | — | — | — |

## Employee (`employee`, rank 90, default scope own records)

Modules: `attendance`, `dashboards`, `employee`, `holidays`, `hr_documents`, `hrms`, `leave`, `overtime`, `payroll`, `performance`, `shifts`, `tasks`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `attendance` | view (own records) | — | — | — | — | — |
| `dashboards` | view (own records) | — | — | — | — | — |
| `employee` | view (own records) | edit (own records) | — | — | — | — |
| `holidays` | view (whole workspace) | — | — | — | — | — |
| `hr_documents` | view (own records) | — | — | — | — | — |
| `hrms` | view (own records) | — | — | — | — | — |
| `leave` | view (own records) | create (own records) | — | — | — | — |
| `overtime` | view (own records) | create (own records) | — | — | — | — |
| `payroll` | view (own records) | — | — | — | — | — |
| `performance` | view (own records) | edit (own records) | — | — | — | — |
| `shifts` | view (own records) | — | — | — | — | — |
| `tasks` | view (own records) | edit (own records) | — | — | — | — |

## Read-Only User (`read_only`, rank 90, default scope team)

Modules: `accounts`, `activities`, `contacts`, `dashboards`, `leads`, `opportunities`, `products`, `reports`, `smartviews`, `tasks`, `tickets`

| Module | Can view | Can change | Can delete | Approves | Reports / export | Other |
|---|---|---|---|---|---|---|
| `accounts` | view (team) | — | — | — | — | — |
| `activities` | view (team) | — | — | — | — | — |
| `contacts` | view (team) | — | — | — | — | — |
| `dashboards` | view (team) | — | — | — | — | — |
| `leads` | view (team) | — | — | — | — | — |
| `opportunities` | view (team) | — | — | — | — | — |
| `products` | view (whole workspace) | — | — | — | — | — |
| `reports` | view (team) | — | — | — | view reports (team) | — |
| `smartviews` | view (team) | — | — | — | — | — |
| `tasks` | view (team) | — | — | — | — | — |
| `tickets` | view (team) | — | — | — | — | — |

