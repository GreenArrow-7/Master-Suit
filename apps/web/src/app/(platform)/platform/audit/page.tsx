import { prisma } from '@/lib/db'; import WorkspaceTable from '@/components/workspace/WorkspaceTable';
export default async function Page() { const rows = await prisma.platformAuditEvent.findMany({ take: 250, orderBy: { occurredAt: 'desc' }, include: { tenant: true, actor: true } }); return <div style={{ display: 'grid', gap: 18 }}><h1>Platform audit</h1><WorkspaceTable headers={['Event', 'Workspace', 'Actor', 'Object', 'Time']} rows={rows.map((r) => [r.event, r.tenant?.displayName ?? 'Global', r.actor?.email ?? 'System', `${r.objectType ?? '—'} ${r.objectId ?? ''}`, r.occurredAt.toLocaleString('en-AE')])} /></div>; }
