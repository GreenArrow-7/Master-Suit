'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation';
export default function WorkspaceControls({ workspaceId, status }: { workspaceId: string; status: string }) { const router = useRouter(); const [busy, setBusy] = useState(false); const [message, setMessage] = useState(''); async function update(body: object) { setBusy(true); setMessage(''); const response = await fetch(`/api/v1/platform/workspaces/${workspaceId}`, { method: 'PATCH', headers: { 'content-type': 'application/json' }, body: JSON.stringify(body) }); const data = await response.json(); setBusy(false); if (!response.ok) { setMessage(data.detail ?? 'Update failed.'); return; } setMessage('Updated.'); router.refresh(); }
  return <div className="lf-card" style={{ padding: 18, display: 'flex', gap: 10, alignItems: 'center', flexWrap: 'wrap' }}><button className="lf-btn" disabled={busy || status === 'ACTIVE'} onClick={() => update({ status: 'ACTIVE', subscriptionState: 'ACTIVE' })}>Reactivate</button><button className="lf-btn lf-btn--secondary" disabled={busy || status === 'SUSPENDED'} onClick={() => update({ status: 'SUSPENDED', subscriptionState: 'SUSPENDED', revokeSessions: true })}>Suspend</button><button className="lf-btn lf-btn--danger" disabled={busy || status === 'ARCHIVED'} onClick={() => update({ status: 'ARCHIVED', subscriptionState: 'CANCELED', revokeSessions: true })}>Archive</button><button className="lf-btn lf-btn--secondary" disabled={busy} onClick={() => update({ revokeSessions: true })}>Revoke sessions</button>{message && <span>{message}</span>}</div>;
}
