# The release gates, in the order a failure is cheapest to diagnose: static
# checks first, then tests, then a build, then the dependency audit.
#
# There is one job, not eight. Every gate needs the same installed node_modules
# and the same generated Prisma client, and splitting them into parallel jobs
# would pay that cost repeatedly to save less time than it spends.
name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: apps/web

jobs:
  verify:
    runs-on: ubuntu-latest
    # E2E below may take 25; the gates before it take ~10 and Build/Audit ~5.
    timeout-minutes: 60

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: leadflow
          POSTGRES_PASSWORD: leadflow
          POSTGRES_DB: leadflow
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U leadflow"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20

    env:
      # NODE_ENV is deliberately NOT set here.
      #
      # `next build` must run as production. Forcing development leaks into the
      # build and breaks prerendering with "Cannot read properties of null
      # (reading 'useContext')" — React resolves to the wrong runtime for the
      # server render. Each step gets what it needs instead: vitest sets `test`,
      # the seed reads the default `development` from lib/env.ts, and the build
      # gets production from Next itself.
      CI: "true"
      # tsc over this codebase (a 6 800-line Prisma schema and its generated
      # client types) needs ~3 GB of heap; Node's default on the runner is ~2 GB
      # and the typecheck gate died OOM-killed mid-mark-compact. Job-wide, so
      # the vitest and next-build steps — the next-largest heaps — get it too.
      NODE_OPTIONS: "--max-old-space-size=6144"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: apps/web/package-lock.json

      # ── Redis, started as a step rather than a service container ────────────
      #
      # Every Compose file in this repository sets `--requirepass`, including the
      # development one, so that no environment leaves the AUTH path unexercised.
      # CI has to match, or the one place the whole suite runs is the one place
      # a password is never sent.
      #
      # A service container cannot do it: GitHub Actions exposes `image`, `env`,
      # `ports` and `options`, and the official redis image takes its password as
      # a *command argument*, which there is no field for. `docker run` has one.
      - name: Redis
        run: |
          docker run -d --name ci-redis -p 6379:6379 redis:7-alpine \
            redis-server --requirepass leadflow
          for i in $(seq 1 30); do
            if docker exec -e REDISCLI_AUTH=leadflow ci-redis redis-cli ping 2>/dev/null | grep -q PONG; then
              echo "redis ready after ${i}s"; exit 0
            fi
            sleep 1
          done
          echo "::error::redis did not become ready"; docker logs ci-redis; exit 1

      - name: Install
        run: npm ci

      # Creates apps/web/.env from apps/web/.env.example, then overwrites the
      # three placeholder secrets with real 32-byte values.
      #
      # `.env` only, deliberately. `.env.test` points DATABASE_URL at a separate
      # `master_saas_test` database that a developer creates locally with
      # `npm run setup`; this runner has one. Vitest prefers `.env.test` when it
      # exists, so generating it here sent every suite at a database that does
      # not exist on the server.
      - name: Generate .env
        run: node scripts/generate-secrets.mjs .env

      # Object storage. Receipt evidence is stored for real by the unit and
      # browser suites, as it is in every environment; without a store here
      # those uploads fail. The credentials are the local defaults `.env` was
      # just generated with, read from it so the two cannot drift. lib/storage.ts
      # creates the bucket on first use. MinIO no longer publishes to Docker Hub
      # (`minio/minio` is refused), so the image is its own registry's copy of the
      # release the local stack runs, pinned by digest.
      - name: Object storage
        run: |
          KEY="$(grep '^S3_ACCESS_KEY_ID=' .env | cut -d= -f2-)"
          SECRET="$(grep '^S3_SECRET_ACCESS_KEY=' .env | cut -d= -f2-)"
          docker run -d --name ci-minio -p 9000:9000 \
            -e MINIO_ROOT_USER="$KEY" -e MINIO_ROOT_PASSWORD="$SECRET" \
            quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z@sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e server /data
          for i in $(seq 1 30); do
            if curl -sf http://127.0.0.1:9000/minio/health/live >/dev/null; then
              echo "object storage ready after ${i}s"; exit 0
            fi
            sleep 1
          done
          echo "::error::object storage did not become ready"; docker logs ci-minio; exit 1

      # tests/tenant/rls.spec.ts refuses to run unless RLS_DATABASE_URL is set and
      # names the *same* connection as DATABASE_URL — testing a role the
      # application never connects as would prove nothing, so it throws rather
      # than skipping. Derived from .env rather than repeated, so the two cannot
      # drift apart.
      #
      # The server integration and browser suites refuse to run unless their
      # target is named explicitly (tests/helpers/isolation.ts,
      # docs/TEST-ISOLATION.md): fixtures connect as the owner role to the same
      # database the application reads, and use the application's Redis
      # database. Both come from `.env` for the same reason. This runner's
      # database is called `leadflow`, which the guard does not recognise as
      # disposable; it is created and destroyed with the job, and the override
      # says so where a reviewer can see it.
      - name: Export test connections
        run: |
          {
            echo "RLS_DATABASE_URL=$(grep '^DATABASE_URL=' .env | cut -d= -f2-)"
            echo "E2E_DATABASE_URL=$(grep '^MIGRATION_DATABASE_URL=' .env | cut -d= -f2-)"
            echo "E2E_REDIS_URL=$(grep '^REDIS_URL=' .env | cut -d= -f2-)"
            echo "E2E_ALLOW_UNMARKED_DATABASE=yes"
          } >> "$GITHUB_ENV"

      - name: Apply migrations
        run: npx prisma migrate deploy

      # Before the demo seed, which would create the same rows and hide a gap. A
      # role can only be granted a permission whose definition exists; this fails
      # when migrations alone leave one the navigation, the monitoring allowlist or
      # a permission check needs (src/lib/security/permissionCatalogue.ts).
      - name: Permission catalogue
        run: npm run check:permissions

      # ── Gate 0a — schema/migration drift ──────────────────────────────────
      #
      # `schema.prisma` and `prisma/migrations` are two descriptions of one
      # database and nothing kept them in step. HrLeaveBalance, HrChecklistTask,
      # HrOffboardingCase, HrSettlementSnapshot and HrEmployeeLocationAssignment
      # were all declared in the schema with no migration creating them, so every
      # code path touching them typechecked cleanly and failed at runtime.
      #
      # The database here has just had every migration replayed into it, so
      # comparing it against the schema compares exactly those two things. Exit
      # code 2 means "they differ".
      - name: Schema drift
        run: npx prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --exit-code

      # ── Gate 0b — tenant isolation is still intact ────────────────────────
      #
      # RLS is the layer that holds when the other two are bypassed, and it is
      # the one that can be silently removed: a migration re-running the catalog
      # sweep with a stale copy of the policy body drops FORCE across every
      # tenant table, and `\d` still shows a policy on each of them.
      #
      # This asserts against the catalog rather than against a list somebody
      # maintains. It found HrOvertimeRequest missing both FORCE and the
      # app.platform_admin branch on its first run.
      - name: Tenant isolation
        run: node scripts/check-rls.mjs

      # ── Gate 0c — raw SQL is inside a tenant transaction ──────────────────
      #
      # The gate above proves every tenant table has a policy. This proves the
      # policies are given something to filter on. The tenant guard is blind to
      # `$queryRaw`, so raw SQL rests on RLS alone, and RLS rests on
      # `app.tenant_id` — which is set transaction-locally. A raw statement
      # issued outside a transaction runs on a pooled connection where that
      # setting is either absent, in which case the policy matches nothing and
      # the caller is told it succeeded, or left over from another request, in
      # which case it matches somebody else's rows.
      #
      # Nothing in the codebase does this today. It is here because the shape
      # that would — a bulk DELETE with no model API to route through — is
      # exactly the shape the retention sweeps take.
      - name: Raw SQL scope
        run: node scripts/check-raw-sql-scope.mjs

      # The demo admin's password. The seed generates one per run and prints it
      # once, which is right for an operator and useless for the E2E step, which
      # needs the same value the seed used. So it is generated here, pinned into
      # the seed through DEMO_PASSWORD (the seed's own override for that), and
      # handed to the browser suite as E2E_DEMO_PASSWORD.
      #
      # Masked before anything else touches it: the seed's summary line prints
      # the password, and the mask turns that — and any later echo — into `***`.
      # $GITHUB_ENV is a file the runner reads, not a line in the log.
      - name: Demo credentials
        run: |
          PASSWORD="$(openssl rand -hex 12)-$(openssl rand -hex 3 | tr a-f A-F)"
          echo "::add-mask::$PASSWORD"
          {
            echo "E2E_DEMO_SLUG=manath-homes"
            echo "E2E_DEMO_EMAIL=amina.alrashid@example.com"
            echo "E2E_DEMO_PASSWORD=$PASSWORD"
          } >> "$GITHUB_ENV"

      # The server integration specs sign in as a member of the `manath-homes`
      # workspace, which only exists once the demo data is seeded. The seed
      # refuses to run against NODE_ENV=production, so it cannot touch a real
      # deployment from here.
      #
      # Under NODE_ENV=development the seed also connects the development mock
      # telephony vendor and gates check-out on lead work for that workspace —
      # the state tests/e2e/business-journey.spec.ts and assisted-call.spec.ts
      # drive. The mock resolves only in development and the isolation guard
      # (tests/helpers/isolation.ts) exempts exactly that provider.
      - name: Seed demo data
        # ALLOW_DEMO_SEED is the seed's own guard: it creates dozens of active
        # logins and refuses to run until the operator confirms the target is
        # disposable. A CI database is created and destroyed with the job.
        env:
          ALLOW_DEMO_SEED: "yes"
          # The seed refuses to run against production, and says so.
          NODE_ENV: development
          DEMO_PASSWORD: ${{ env.E2E_DEMO_PASSWORD }}
        run: npm run db:seed

      # ── Gate 1 ────────────────────────────────────────────────────────────
      - name: Typecheck
        run: npx tsc --noEmit

      # ── Gate 2 ────────────────────────────────────────────────────────────
      - name: Lint
        run: npm run lint

      # ── Gate 3 — format check ─────────────────────────────────────────────
      #
      # Absent since 2026-08-15, when `npm run format:check` failed on 678 files
      # and enforcing it would have forced a 678-file reformat into whichever PR
      # ran first, while several branches were in flight.
      #
      # Most of that 678 was not source. `.prettierignore` did not list
      # `.next-prod` — the local production build's output directory, which
      # vendors dependencies with their own sources — so once anybody had run a
      # production build the check was measuring third-party packages. With that
      # one line added the real number was 99 files, reformatted in the same
      # commit that re-enables this. (2026-08-20)
      - name: Format check
        run: npm run format:check

      # ── Gate 3b — the README's schema counts ──────────────────────────────
      #
      # It claimed "79 models, 25 enums, 119 indexes" against a schema holding
      # 192, 103 and 332. The first file a newcomer opens was wrong, and a reader
      # who catches one wrong number stops trusting the rest of the page.
      #
      # Counting by hand and pasting the result is how it got that way, so the
      # numbers are generated between markers and this checks the paste.
      - name: README schema counts
        run: node scripts/schema-stats.mjs --check

      # ── Gate 3c — the monitoring stack and the thing it monitors ───────────
      #
      # Every failure this catches is a silent one. A renamed scrape job turns
      # ApplicationDown into a rule that can never fire; a queue missing from
      # QueueHasNoConsumer is a queue whose worker can exit unnoticed, which is
      # exactly how the worker process came to be dead in production for months;
      # an overlay that stops clearing the `observability` profile is a
      # deployment that quietly runs with no scraper at all.
      #
      # None of those break a test or a build. They only show up as monitoring
      # that was never going to say anything.
      - name: Observability drift
        run: npm run check:observability

      # ── Gate 3d — Redis has a password, everywhere, and it agrees ──────────
      #
      # Both failures this catches present as jobs that never run rather than as
      # a configuration error: a Compose file that drops `--requirepass`, and an
      # env file whose REDIS_URL still carries yesterday's password. The second
      # is the likelier one, because the password is written twice — bare for
      # the container's `--requirepass`, and as userinfo for the application.
      - name: Redis auth
        run: npm run check:redis-auth

      # ── Gate 3e — the face service's authentication gate ───────────────────
      #
      # The only thing between the network and a service that turns camera
      # frames into biometric vectors is one bearer token, and that comparison
      # has already been wrong twice: once accepting everything when the
      # variable was unset, once using `!=` and leaking the token through
      # timing. Rotation support added a third way to be wrong — short-circuiting
      # on the first of two candidates — so it is tested rather than trusted.
      #
      # Plain python3, no pytest and nothing to install: apps/face/tokens.py
      # imports `hmac` and stops, precisely so this can run on a runner that has
      # never seen onnxruntime.
      - name: Face token gate
        run: python3 ../face/test_tokens.py

      # ── Gate 3f — the backup can come home ────────────────────────────────
      #
      # scripts/backup-ship.sh and scripts/restore-verify.sh decide whether this
      # deployment is recoverable, and had no tests at all. One of the things
      # the header of the first asserted was false: that the weekly unit ran a
      # restore against a copy pulled back from the remote. It ran one against
      # the local directory, and nothing could pull a copy back, because that
      # code did not exist.
      #
      # This drives the `local` transport end to end — ship, list, pull,
      # compare — plus the four refusals. The s3, rclone and rsync branches need
      # a real remote and are not covered; what is covered is the shell, which
      # is where this feature's failures have actually lived.
      - name: Backup round trip
        run: scripts/test-backup-roundtrip.sh

      # ── Gate 4 ────────────────────────────────────────────────────────────
      #
      # A skipped test is a failure here. `describe.skip` and `it.skip` are the
      # cheapest way to make a suite green while deleting its coverage, and a
      # summary line reading "0 failed" hides them completely — so the run is
      # rejected if the reporter mentions any.
      - name: Test
        run: |
          set -o pipefail
          npx vitest run --reporter=default 2>&1 | tee vitest.log
          if grep -Eq '[0-9]+ (skipped|todo)' vitest.log; then
            echo "::error::Skipped or todo tests are not allowed. See the summary above."
            exit 1
          fi

      # ── Gate 5 ────────────────────────────────────────────────────────────
      #
      # Integration specs that drive a running application. They own their own
      # server: tests/server/globalSetup.ts starts one on a free port, waits for
      # it to answer, and stops it again whether the run passes or fails. Gate 4
      # used to collect these, and failed on every clean run because nothing had
      # started a server yet.
      - name: Integration (server)
        run: npm run test:server

      # ── Gate 6 ────────────────────────────────────────────────────────────
      #
      # The browser is cached and installed in its own step, because this is
      # where CI kept failing without a test ever running.
      #
      # `playwright install --with-deps` shells out to apt-get. When the Azure
      # Ubuntu mirror went unreachable it retried, fell back to archive.ubuntu.com,
      # and then sat silent for 24 minutes until the 30-minute job timeout killed
      # the run — taking Build and Audit with it. Nothing was wrong with the code
      # and no test had started.
      #
      # So: cache the browser on its version, install without touching apt, and
      # reach for apt only if Chromium genuinely cannot launch. The bounded
      # timeout means a stall costs six minutes and names itself, instead of
      # eating the job's whole budget.
      # ── E2E, only where it earns its cost ─────────────────────────────────
      #
      # The browser suite is by far the most expensive gate — roughly two thirds
      # of the job's minutes — and this repository is private on a plan with a
      # monthly Actions allowance. Twenty-nine runs in one day, across several
      # branches in flight at once, spent about a quarter of that allowance and
      # left runs queueing for the best part of an hour before a runner picked
      # them up.
      #
      # So it runs where a regression would actually reach someone: a pull
      # request targeting `main`, and every push to `main` itself. A branch
      # pushed for review against any other base still gets typecheck, lint,
      # unit, integration, build and audit — everything except the browser.
      # Re-target the PR at `main` and the browser gate runs before it can merge.
      - name: Playwright version
        id: playwright
        if: github.event_name == 'push' || github.base_ref == 'main'
        run: echo "version=$(node -p "require('@playwright/test/package.json').version")" >> "$GITHUB_OUTPUT"

      - name: Cache Playwright browser
        if: github.event_name == 'push' || github.base_ref == 'main'
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ steps.playwright.outputs.version }}

      - name: Install Playwright browser
        if: github.event_name == 'push' || github.base_ref == 'main'
        timeout-minutes: 6
        run: |
          npx playwright install chromium
          # The runner image already ships Chromium's shared libraries. Only if
          # that turns out to be false is apt worth the risk.
          node -e "require('@playwright/test').chromium.launch().then((b) => b.close())" || npx playwright install-deps chromium

      # The BullMQ worker, which the dev server does not start. The business
      # journey waits on call analysis and audit, and both run here; without it
      # the spec sits at "analysis did not complete" for ninety seconds and then
      # fails on something that is not a defect.
      #
      # Backgrounded, and gated on its own "workers started" line so a worker
      # that cannot attach to its queues fails this step by name rather than
      # the suite two minutes later. Output goes to a file, not the job log,
      # and the file rides along with the E2E report when the suite fails.
      # Stopped in its own step below, whether E2E passed or not.
      #
      # Execution on: the account-deletion journey hands this worker a scoped
      # erasure job and fails loudly if the job comes back `disabled`. The
      # switch is off by default for a reason (see env.ts); here the database
      # is disposable and exists for this run only.
      - name: Worker
        if: github.event_name == 'push' || github.base_ref == 'main'
        env:
          ACCOUNT_DELETION_EXECUTION_ENABLED: "true"
        run: |
          npm run worker > worker.log 2>&1 &
          WORKER=$!
          # Liveness by npm's own pid, not by pattern: on the first tick npm has
          # not yet spawned tsx, and a pattern match would read that as "died".
          for i in $(seq 1 90); do
            if grep -q 'workers started' worker.log; then echo "worker ready after ${i}s"; exit 0; fi
            kill -0 "$WORKER" 2>/dev/null || break
            sleep 1
          done
          echo "::error::the worker did not start"; tail -n 40 worker.log; exit 1

      - name: E2E
        id: e2e
        if: github.event_name == 'push' || github.base_ref == 'main'
        # 86 tests on one worker against the dev server. At 15 the suite no
        # longer fits: main finished in 15.0 (run 34970295696) and the release
        # closeout's runs timed out at 53 and 69 of 86 with nothing failing
        # (34974531433, 34977699059). A limit, not an assertion: every test
        # still has to pass.
        #
        # The demo-workspace specs (E2E_DEMO_*, exported by "Demo credentials")
        # add about a minute on a warm server; face-attendance still skips,
        # because its frames are never committed and the face sidecar is not
        # here.
        timeout-minutes: 35
        env:
          # The account-deletion journey uses the worker started above instead
          # of an in-process one: two consumers on one queue would race.
          E2E_DELETION_WORKER: external
        run: npx playwright test

      # The worker must not outlive the suite: Build below refuses to run with
      # any E2E process still alive, and this is the counterpart of Playwright
      # stopping its own dev server. SIGTERM, so it drains rather than dies.
      - name: Stop worker
        if: always() && (github.event_name == 'push' || github.base_ref == 'main')
        run: |
          pkill -TERM -f 'src/workers/index.ts' || true
          for i in $(seq 1 30); do pgrep -f 'src/workers/index.ts' >/dev/null || break; sleep 1; done

      # Not a gate — the diagnosis for the one above.
      #
      # E2E is the only step here whose failure cannot be read from the job log
      # by anything but a person with the Actions UI open: the log is long, the
      # useful part is thousands of lines from the end, and the API routes that
      # serve it are not reachable from every environment that needs to fix a
      # red build. This suite has already gone red twice on failures nobody
      # could see, each diagnosed by re-running it rather than by reading it.
      #
      # `playwright.config.ts` adds the `html` reporter under CI so there is a
      # self-contained report to take; `use` already retains a trace and a
      # screenshot on failure and the reporter copies both into it.
      - name: E2E report
        # Precisely "E2E ran and failed" — `failure()` alone would also fire
        # when an earlier gate failed and E2E never ran, uploading nothing.
        if: always() && steps.e2e.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          # The attempt number is load-bearing: re-running a failed job reuses
          # the run id, and upload-artifact@v4 fails outright on a duplicate
          # artifact name. Re-running is exactly what happens to this step.
          name: playwright-report-${{ github.run_id }}-attempt-${{ github.run_attempt }}
          # Repo-relative. `defaults.run.working-directory` applies to `run:`
          # steps and not to an action's inputs, so `playwright-report/` on its
          # own would resolve at the repository root and quietly upload nothing.
          # The worker's log rides along: a journey that stalls on analysis is
          # diagnosed there, not in a screenshot.
          path: |
            apps/web/playwright-report/
            apps/web/worker.log
          # Long enough to diagnose a red build, short enough that screenshots
          # of a seeded workspace do not sit in the account for a quarter.
          retention-days: 7
          if-no-files-found: error

      # ── Gate 7 ────────────────────────────────────────────────────────────
      - name: Build
        # The E2E step's dev server writes `.next/dev/types/validator.ts`, which tsconfig
        # includes for typegen, and Playwright's teardown can stop the server mid-write.
        # `next build`'s TypeScript pass then fails on a truncated file that has nothing
        # to do with the code (seen on PR #59: TS1002 "Unterminated string literal" at a
        # line a clean build never produces). A production build takes no input from dev
        # output, so none is left for it to read.
        run: |
          # Playwright's webServer stops `next dev` when the suite ends. Wait for that to
          # be true before touching its output, bounded, and say so if it is not.
          for i in $(seq 1 30); do pgrep -f 'next dev' >/dev/null || break; sleep 1; done
          if pgrep -f 'next dev' >/dev/null; then echo "::error::a next dev process is still running after E2E"; exit 1; fi
          # Its sibling: "Stop worker" sent SIGTERM and waited; a worker still here did not drain.
          if pgrep -f 'src/workers/index.ts' >/dev/null; then echo "::error::the worker is still running after E2E"; exit 1; fi
          # Only the dev server's own output. Test traces and the Playwright report live in
          # test-results/ and playwright-report/, which this does not touch.
          rm -rf .next/dev
          npm run build

      # ── Gate 8 ────────────────────────────────────────────────────────────
      #
      # Production dependencies only. A dev-only advisory does not ship.
      - name: Audit
        run: npm audit --omit=dev --audit-level=high
